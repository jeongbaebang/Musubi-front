import Box, { BoxProps } from "@mui/material/Box";
import { styled } from "@mui/material/styles";

// const ThemeBox = styled(Box)<BoxProps>(({ theme }) => ({}));

// const StyledBox = <T extends BoxProps>(props: T) => {
//   return <ThemeBox {...props}>{props.children}</ThemeBox>;
// };

export { Box };
